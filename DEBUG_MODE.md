# Debug 模式使用说明

## 概述

这个项目现在支持一个新的 debug 模式，可以自动获取微信密钥、保存账户信息并定期解密微信数据库。

## 使用方法

### 启动 debug 模式

```bash
go run main.go -debug
```

或者先编译再运行：

```bash
go build .
./chatlog.exe -debug
```

### 功能特性

1. **自动获取微信密钥**：自动检测并获取所有在线微信账户的数据密钥和图片密钥
2. **智能缓存**：使用 RBblot 数据库保存账户信息，避免重复获取密钥
3. **定期解密**：每分钟自动解密一次微信数据库到 `storage` 文件夹
4. **缓存过期**：24小时后自动重新获取密钥信息

### 工作流程

1. **首次运行**：
   - 检测所有在线的微信账户
   - 获取每个账户的数据密钥和图片密钥
   - 将账户信息保存到 `rtpsystem.dat` 文件中
   - 立即执行一次数据库解密
   - 启动定时器，每分钟解密一次

2. **后续运行**：
   - 从缓存中读取账户信息
   - 检查缓存是否过期（24小时）
   - 如果过期则重新获取密钥
   - 继续定期解密流程

### 输出目录结构

```
storage/
├── 账户1/
│   ├── db_storage/
│   │   ├── message/
│   │   │   ├── message_0.db
│   │   │   └── message_1.db
│   │   ├── session/
│   │   │   └── session.db
│   │   └── ...
│   └── ...
└── 账户2/
    └── ...
```

### 日志输出

debug 模式会输出详细的日志信息：

- `[启动]` - 程序启动信息
- `[缓存]` - 缓存相关操作
- `[处理]` - 账户处理过程
- `[密钥]` - 密钥获取过程
- `[成功]` - 成功操作提示
- `[保存]` - 数据保存信息
- `[解密]` - 解密操作结果
- `[定期解密]` - 定时解密标记
- `[错误]` - 错误信息
- `[警告]` - 警告信息
- `[跳过]` - 跳过操作说明

### 注意事项

1. **微信状态**：只有在线状态的微信账户才能获取密钥
2. **权限要求**：获取密钥可能需要管理员权限
3. **性能影响**：密钥获取过程可能会暂时影响微信响应速度
4. **存储空间**：解密的数据库文件会占用相当的磁盘空间
5. **数据安全**：请妥善保管生成的密钥和数据库文件

### 停止程序

按 `Ctrl+C` 可以停止程序运行。

### 故障排除

1. **未找到微信实例**：确保微信正在运行且已登录
2. **获取密钥失败**：尝试以管理员权限运行程序
3. **解密失败**：检查微信版本是否受支持
4. **权限不足**：确保对目标目录有写入权限

## 技术实现

- **账户管理**：使用项目原有的 wechat 模块
- **密钥提取**：调用 key extractor 获取密钥
- **数据持久化**：使用 RBblot 进行账户信息缓存
- **文件监控**：使用 filemonitor 包查找数据库文件
- **解密功能**：使用 decrypt 模块解密数据库
- **定时任务**：使用 Go 的 time.Ticker 实现定期执行